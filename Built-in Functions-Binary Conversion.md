# Built-in Functions -Binary Conversion Using Built-in Functions in Python

## 🎯 Aim
To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions.

## 🧠 Algorithm
1. Assign the value `16` to a variable `a`.
2. Use the built-in `bin()` function to convert the number to binary.
3. Print the result.

## 🧾 Program

```p
a = 16
print(bin(a))
```

## Output
<img width="1830" height="458" alt="image" src="https://github.com/user-attachments/assets/fc092d17-8acf-4c17-8f07-3f38efaf2e5c" />

## Result

Thus, the Python program to convert the number 16 into its binary representation using the built-in bin() function was successfully executed and verified.
